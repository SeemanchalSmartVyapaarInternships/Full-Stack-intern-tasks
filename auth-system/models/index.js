// ============================================================
// Model Loader — centralises all Sequelize models
// ============================================================

const User = require('./User');
const Otp = require('./Otp');

module.exports = { User, Otp };
